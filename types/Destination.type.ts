type Destination = {
    id: string,
    city: string,
    state: string | null,
    country: string,
    airportCode: string,
    airportName: string,
    // TODO: add prefecture/region?
}
export default Destination